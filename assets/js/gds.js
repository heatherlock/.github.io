document.addEventListener('DOMContentLoaded', (event) => {
    var frame = document.getElementById('GDS');
    let width = frame.style.width;
    let height = width*.753;
    frame.style.height = height;
    console.log("here")
});
